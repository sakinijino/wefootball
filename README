Setup:
 * gem install rails -v 2.2.2 or rake rails:freeze:edge TAG=rel_2_2_2
 * gem install sqlite3-ruby (for test with sqlite3, if necessary)
 * install ImageMagick
 * git clone wefootball.git
 * rake gems:build 

Run Tests:
 * rake db:migrate
 * rake test
