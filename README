Setup:
 * gem install rails -v 2.1 or rake rails:freeze:edge TAG=rel_2_1
 * gem install sqlite3-ruby (for test with sqlite3, if necessary)
 * install ImageMagick
 * git clone wefootball.git
 * rake gems:build 

Run Tests:
 * rake db:migrate
 * rake test
