<h1><%=h @post.title %></h1>

<%= render(:partial => 'shared/post', :object => @post)%>

<%= render(:partial => 'shared/post', :collection => @post.replies, :locals => {:parent => @post})%>

<%= error_messages_for :reply %>
  
<% form_for :reply, :url => post_replies_path(@post) do |f| %>
  <p>
    <b>内容</b><br />
    <%= f.text_area :content %>
  </p>

  <p>
    <%= f.submit "提交" %>
  </p>
<% end %>
