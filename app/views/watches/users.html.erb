<div id="wide_left_content" class="sect">
  <h2><%=h(@title)%></h2>
  <%= user_icon(@users)%>
  <div class="cb"></div>
  <%= will_paginate @users %>
</div>

<div id="narrow_right_content" class="sect">
  <h2></h2>
  <ul class="sidebar_menu">
    <li>
      > <%= link_to '返回看球活动的页面', @watch %>
    </li>
  </ul>
</div>
<div class="cb"></div>
